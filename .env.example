# Application environment variables
#
# Note:
# - The CLI reads `SUPABASE_DB_URL` or `DATABASE_URL` for Postgres connections.
# - .env files are NOT auto-loaded by Python. Either export these in your shell
#   or use a loader (e.g., `direnv`, `dotenv` CLI, or add python-dotenv).
# - If your provider requires SSL, append `&sslmode=require` (or `?sslmode=require`
#   if there are no existing query params).

# Primary connection string to Supabase/Postgres
SUPABASE_DB_URL="postgresql://localhost:5432/postgres?user=userd&password=password"

# Optional: Alternative variable name recognized by the app
#DATABASE_URL="postgresql://user:pass@host:5432/dbname?sslmode=require"

# Optional: Search behavior tuning (reserved for future use)
#LAW_SEARCH_LIMIT="10"

# Optional: Base URL for the law MCP server powering legal tool calls
#LAW_MCP_BASE_URL="http://127.0.0.1:8000/mcp"

# LLM configuration (for `ask` agent)
#OPENAI_API_KEY="sk-..."               # required to use LLM
#OPENAI_MODEL="gpt-5-mini-2025-08-07"   # default if unset
#OPENAI_BASE_URL="https://api.openai.com/v1"
#OPENAI_TEMPERATURE=""                 # optional; leave empty for provider default

# LangSmith tracing
# LANGSMITH_TRACING=true
#LANGSMITH_API_KEY="ls_api_key"
#LANGSMITH_PROJECT="my-langsmith-project"
#LANGSMITH_ENDPOINT="https://api.smith.langchain.com"  # optional override
#LAW_LANGSMITH_ENABLED="1"                               # set 0 to disable when creds exist

# Workspace API configuration
#LAW_SHARE_DB_URL="postgresql://localhost:5432/workspace_db?user=user&password=pass"
#LAW_ENABLE_AUDIT="true"          # enable audit logging (default: true)
#LAW_ENABLE_BUDGET_CHECK="true"   # enable budget checks (default: true)

# Cloudflare R2 Object Storage (optional, for file uploads)
# Get credentials from your Cloudflare dashboard:
# https://dash.cloudflare.com/ → R2 → Overview → Manage R2 API Tokens
#R2_ENDPOINT_URL="https://<account_id>.r2.cloudflarestorage.com"
#R2_ACCESS_KEY_ID="<your_r2_access_key_id>"
#R2_SECRET_ACCESS_KEY="<your_r2_secret_access_key>"
#R2_BUCKET_NAME="law-workspace-files"
#R2_PUBLIC_DOMAIN=""                    # optional: custom domain for public access
#R2_MAX_FILE_SIZE="104857600"           # max file size in bytes (default: 100MB)
#R2_PRESIGNED_URL_EXPIRY="3600"         # presigned URL expiry in seconds (default: 1 hour)
